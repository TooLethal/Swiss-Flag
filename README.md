# 🇨🇭 CSS Flag of Switzerland

A simple **HTML + CSS project** that recreates the **national flag of Switzerland** using styled div elements. The flag features a red square background with a bold white cross centered both vertically and horizontally.

## ✨ Features
- **Accurate Colors**  
  - Red → `#FF0002`  
  - White → `#FFFFFF`  

- **Flag Layout**  
  - Square red background (`.flag-swiss`).  
  - Two white rectangles (`.cross1` and `.cross2`) overlap to form the Swiss cross.  
  - Cross is perfectly centered using `transform: translate(-50%, -50%)`.  

- **Customizable Variables**  
  - Colors defined in `:root` for easy adjustments.  
  - Flag size and cross dimensions can be modified for different proportions.  

- **Responsive Centering**  
  - Flexbox centers the flag both vertically and horizontally on the page.  

- **Minimal Structure**  
  - Simple HTML with `.flag-swiss` container and two `.cross` divs.  
  - Lightweight CSS for layout and coloring.  

## 🛠️ Tech Stack
- **HTML5**  
- **CSS3 (Flexbox + Positioning + Variables)**  

## 🚀 Use Cases
- Practicing CSS layout and positioning  
- Demonstrating how to build flags with pure CSS  
- Educational projects for learning absolute positioning  
- Creative UI elements for themed websites  

## 📂 Project Structure
```
index.html   # Main HTML structure
style.css    # Flag styling and layout
```
